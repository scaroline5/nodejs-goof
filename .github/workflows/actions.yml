name: my-first-github-action
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: uname -a
      - run: id
      - run: env
